name: dio_http_cache_example
description: A demo of the dio_http_cache package.
version: 1.0.0+1
publish_to: none

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.3+1
  dio_http_cache:
    git:
      url: https://github.com/aungmyopaing890/dio_http_cache.git
      ref: master

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
